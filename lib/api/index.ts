// Public API surface for the app. Import from '@/lib/api' everywhere so the
// underlying implementation (mock vs real backend) stays swappable.
export * from './types'
export * from './services'
export { CATEGORY_KEYWORDS, PRODUCT_CATALOG } from './mock-data'
